name: 🚀 GBSB4U WebBrowser APK Build (Gradle Updated) 
on: [push, workflow_dispatch] 
 
jobs: 
  build: 
    runs-on: ubuntu-latest 
     
    steps: 
    # ✅ OFFICIAL GRADLE ACTION (Web Search #1) 
    - uses: gradle/gradle-build-action@v3 
     
    # ✅ JAVA 17 EXPLICIT (Web Search #2) 
    - uses


