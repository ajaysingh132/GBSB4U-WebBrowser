        yes | sdkmanager --licenses # सभी SDK लाइसेंस स्वीकार करें 
        sdkmanager --update       # SDK टूल्स को अपडेट करें 
         
    # ✅ FIX 4: Exact SDK Versions - कोई इंस्टॉल failure नहीं। 
    - name: 🔧 Install SDK Components (GBSB4U Compatible) 
      run: | 
        sdkmanager "platforms;android-34" "build-tools;34.0.0" # Android 14 SDK इंस्टॉल करें 
        sdkmanager "platforms;android-33" "build-tools;33.0.2" # Android 13 SDK भी इंस्टॉल करें (यदि आवश्यकता हो) 
         
    # ✅ FIX 5: Gradle Build - अब यह सुनिश्चित है कि सभी आवश्यक सेटिंग्स मौजूद हैं। 
    - name: 🚀 Build Debug and Release APKs 
      run: ./gradlew assembleDebug assembleRelease # Debug और Release दोनों APKs बनाएं 
       
    # ✅ APK Upload - बनाए गए APKs को Artifact के रूप में अपलोड करें। 
    - name: 📤 Upload GBSB4U APKs 
      uses: actions/upload-artifact@v4 
      with: 
        name: GBSB4U-WebBrowser-APKs # Artifact का नाम 
        path: | 
          app/build/outputs/apk/debug/app-debug.apk   # Debug APK का पाथ 
          app/build/outputs/apk/release/app-release.apk # Release APK का पाथ 
        retention-days: 30 # Artifact कितने दिन तक GitHub पर रहेगा 


